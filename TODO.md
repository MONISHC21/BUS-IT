# Flutter to GitHub Pages Deployment TODO

## Pending Steps:
- [ ] 1. Run `flutter pub get` to install dependencies
- [ ] 2. Build web app: `flutter build web --release --base-href '/BUS-IT/'`
- [ ] 3. Git init: `git init`, `git add .`, `git commit -m 'Initial Flutter commit'`
- [ ] 4. Setup remote: `git branch -M main`, `git remote add origin https://github.com/MONISHC21/BUS-IT.git`, `git push -u origin main`
- [ ] 5. Create docs: `mkdir docs`, copy `build\web\*` to `docs\`
- [ ] 6. Deploy commit: `git add .`, `git commit -m 'Deploy to GitHub Pages /docs'`, `git push`
- [ ] 7. Manual: Enable GitHub Pages on main:/docs

**Live URL after step 7: https://MONISHC21.github.io/BUS-IT/**

**Notes:** Windows commands used. Repo overwrite OK per approval.
